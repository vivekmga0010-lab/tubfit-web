{
  "name": "tubfit-web",
  "private": true,
  "scripts": {
    "install": "npm --prefix frontend install && npm --prefix backend install",
    "build": "npm --prefix frontend run build && node scripts/build.js",
    "start": "npm --prefix backend run start",
    "deploy": "npm run build && pm2 restart tubfit || pm2 start backend/server.js --name tubfit"
  }
}
